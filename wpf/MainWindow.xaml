<Window x:Class="test_register_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:localmodel="clr-namespace:Model"      
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight">
    <Window.Resources>
        <!-- VarClass Template-->
        <DataTemplate DataType="{x:Type localmodel:VarClass}">
            <Border BorderBrush="Orange" BorderThickness="2" Margin="2,2,2,2">
                <StackPanel Orientation="Horizontal" IsEnabled="{Binding IsEnabled}">
                    <TextBlock Text="{Binding Name}"  TextAlignment="Right" />
                    <TextBlock Text=" : 0X" />
                    <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=LostFocus,
                                Converter={StaticResource NumberToHexString}, ConverterParameter=2}" />
                </StackPanel>
            </Border>
        </DataTemplate>
        <!-- FLagClass Template-->
        <DataTemplate DataType="{x:Type localmodel:FlagClass}">
            <Border BorderBrush="Orange" BorderThickness="2">
                <StackPanel Orientation="Horizontal" IsEnabled="{Binding IsEnabled}">
                    <TextBlock Text="{Binding Name}"  TextAlignment="Right" />
                    <TextBlock Text=" : "  />
                    <CheckBox IsChecked="{Binding Value, Mode=TwoWay, UpdateSourceTrigger =PropertyChanged}"  />
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <StackPanel>
        <ItemsControl ItemsSource="{Binding RegList}" Margin="5,5,5,5" >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Blue" BorderThickness="2">
                        <StackPanel Orientation="Horizontal" Margin="2,2,2,2">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox x:Name="ItemControlCheckBox" IsChecked="{Binding IsEnabled}"/>
                                <StackPanel IsEnabled="{Binding IsChecked, ElementName=ItemControlCheckBox}" Orientation="Horizontal">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"  TextAlignment="Right" />
                                        <StackPanel Orientation="Horizontal">
                                            <Button Command="{Binding  DataContext.ReadRegisterCommand, ElementName=TreeviewContainer}" 
                                    CommandParameter="{Binding Name}" Content="R" Height="20"/>
                                            <Button Command="{Binding  DataContext.WriteRegisterCommand, ElementName=TreeviewContainer}" 
                                    CommandParameter="{Binding Name}" Content="W" Height="20"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <TextBlock Text=" : 0X" />
                                    <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=LostFocus,
                                Converter={StaticResource NumberToHexString}, ConverterParameter=2}" />
                                </StackPanel>
                            </StackPanel>
                            <ItemsControl ItemsSource="{Binding Items}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- https://www.codemag.com/article/1401031/Using-the-WPF-Tree-View-with-Multiple-Levels -->
        <!--- https://wpf-tutorial.com/treeview-control/treeview-data-binding-multiple-templates/ -->
        <!-- https://wpf-tutorial.com/be/85/the-treeview-control/treeview-data-binding-and-multiple-templates/ -->
        <GroupBox Header="Tree View" >
            <TreeView ItemsSource="{Binding RegList}" x:Name="TreeviewContainer">
                <TreeView.Resources>
                    <!--Register Template -->
                    <HierarchicalDataTemplate DataType="{x:Type localmodel:RegClass}" ItemsSource="{Binding Items}">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox x:Name="TreeViewCheckBox" IsChecked="{Binding IsEnabled}"/>
                            <StackPanel IsEnabled="{Binding IsChecked, ElementName=TreeViewCheckBox}" Orientation="Horizontal">
                                <Button Command="{Binding  DataContext.ReadRegisterCommand, ElementName=TreeviewContainer}" 
                                    CommandParameter="{Binding Name}" Content="R" />
                                <Button Command="{Binding  DataContext.WriteRegisterCommand, ElementName=TreeviewContainer}" 
                                    CommandParameter="{Binding Name}" Content="W"/>
                                <TextBlock Text="{Binding Name}"  TextAlignment="Right" />
                                <TextBlock Text=" : 0X" />
                                <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=LostFocus,
                                Converter={StaticResource NumberToHexString}, ConverterParameter=2}" />
                            </StackPanel>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </GroupBox>
        <Button Command="{Binding DumpRegsCommand}">Dump</Button>
        <Border BorderBrush="blue" BorderThickness="2" Height="200" MinWidth="400">
            <ScrollViewer>
                <TextBox Text="{Binding ConsoleText}"></TextBox>
            </ScrollViewer>
        </Border>
    </StackPanel>
</Window>
