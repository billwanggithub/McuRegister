<Window x:Class="test_register_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="Model"
        xmlns:localmodel="clr-namespace:Model"
        mc:Ignorable="d"
        Title="MainWindow" SizeToContent="WidthAndHeight">
    <Window.Resources>
        <!-- VarClass Template-->
        <DataTemplate DataType="{x:Type localmodel:VarClass}">
            <Border BorderBrush="Orange" BorderThickness="2" Margin="2,2,2,2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" Width="40" TextAlignment="Right" />
                    <TextBlock Text=" : 0X" Width="40"/>
                    <TextBox Text="{Binding Value , Mode=TwoWay, UpdateSourceTrigger=LostFocus, StringFormat=X2}" Width="40"/>
                </StackPanel>
            </Border>
        </DataTemplate>
        <!-- FLagClass Template-->
        <DataTemplate DataType="{x:Type localmodel:FlagClass}">
            <Border BorderBrush="Orange" BorderThickness="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" Width="40" TextAlignment="Right" />
                    <TextBlock Text=" : " Width="40" />
                    <CheckBox IsChecked="{Binding Value, Mode=TwoWay, UpdateSourceTrigger =PropertyChanged}" Width="40" />
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <StackPanel>
        <ItemsControl ItemsSource="{Binding RegList}" Margin="5,5,5,5" >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Blue" BorderThickness="2">
                        <StackPanel Orientation="Horizontal" Margin="2,2,2,2">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Margin="2,2,2,2"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="0X "></TextBox>
                                    <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=LostFocus, StringFormat=X2}"></TextBox>
                                </StackPanel>
                            </StackPanel>
                            <ItemsControl ItemsSource="{Binding Items}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- https://www.codemag.com/article/1401031/Using-the-WPF-Tree-View-with-Multiple-Levels -->
        <!--- https://wpf-tutorial.com/treeview-control/treeview-data-binding-multiple-templates/ -->
        <!-- https://wpf-tutorial.com/be/85/the-treeview-control/treeview-data-binding-and-multiple-templates/ -->
        <GroupBox Header="Tree View">
            <TreeView ItemsSource="{Binding RegList}">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type localmodel:RegClass}" ItemsSource="{Binding Items}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Width="40" TextAlignment="Right" />
                            <TextBlock Text=" : 0X" Width="40"/>
                            <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=LostFocus, StringFormat=X2}" Width="40"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </GroupBox>
        <Button Command="{Binding DumpRegsCommand}">Dump</Button>
        <Border BorderBrush="blue" BorderThickness="2" Height="200" MinWidth="400">
            <ScrollViewer>
                <TextBlock Text="{Binding ConsoleText}"></TextBlock>
            </ScrollViewer>
        </Border>
    </StackPanel>
</Window>
